{
  "name": "@tachui/connectrpc",
  "version": "0.11.6",
  "private": true,
  "description": "Optional ConnectRPC adapter for tachUI: maps generated Connect service methods onto @tachui/query",
  "homepage": "https://tachui.dev/",
  "type": "module",
  "sideEffects": false,
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "!dist/.vite"
  ],
  "scripts": {
    "build": "rm -rf dist tsconfig.build.tsbuildinfo && vite build && tsc --project tsconfig.build.json",
    "build:dev": "rm -rf dist tsconfig.build.tsbuildinfo && vite build --mode development && tsc --project tsconfig.build.json",
    "build:prod": "rm -rf dist tsconfig.build.tsbuildinfo && vite build --mode production && tsc --project tsconfig.build.json",
    "dev": "vite build --watch",
    "test": "vitest run",
    "type-check": "tsc --project tsconfig.type-check.json",
    "clean": "rm -rf dist",
    "lint": "oxlint src",
    "size": "node ../../tools/check-size-budget.mjs @tachui/connectrpc",
    "valid": "clear && vitest run && bun run type-check && bun run lint && bun run build"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "dependencies": {
    "@tachui/query": "0.8.36"
  },
  "peerDependencies": {
    "@bufbuild/protobuf": "^2.2.0",
    "@connectrpc/connect": "^2.0.0"
  },
  "devDependencies": {
    "@bufbuild/protobuf": "^2.15.0",
    "@connectrpc/connect": "^2.2.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.8.0",
    "vite": "^7.1.1",
    "vitest": "^3.2.4"
  },
  "keywords": [
    "tachui",
    "connectrpc",
    "connect",
    "protobuf",
    "query",
    "reactive"
  ],
  "author": "tachUI Team",
  "license": "MPL-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/tach-UI/tachUI",
    "directory": "packages/connectrpc"
  },
  "publishConfig": {
    "access": "public"
  }
}
