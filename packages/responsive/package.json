{
  "name": "@tachui/responsive",
  "version": "0.8.0-alpha",
  "description": "Advanced responsive design patterns and utilities for tachUI framework",
  "homepage": "https://tachui.dev/",
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.mjs",
      "require": "./dist/index.js"
    },
    "./modifiers": {
      "types": "./dist/modifiers/index.d.ts",
      "import": "./dist/modifiers/index.mjs",
      "require": "./dist/modifiers/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rm -rf dist && vite build && vite build --config vite.config.cjs.ts && tsc -p tsconfig.build.json --emitDeclarationOnly",
    "build:dev": "rm -rf dist && vite build --mode development && vite build --config vite.config.cjs.ts && tsc -p tsconfig.build.json --emitDeclarationOnly",
    "build:prod": "rm -rf dist && vite build --mode production && vite build --config vite.config.cjs.ts && tsc -p tsconfig.build.json --emitDeclarationOnly",
    "dev": "vite build --watch",
    "valid": "clear && vitest run && pnpm type-check && pnpm lint && pnpm build",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest",
    "test:ci": "npm run test:run",
    "type-check": "tsc --project tsconfig.type-check.json",
    "clean": "rm -rf dist",
    "lint": "oxlint src"
  },
  "dependencies": {
    "@tachui/core": "workspace:*",
    "@tachui/modifiers": "workspace:*",
    "@tachui/registry": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/jsdom": "^21.1.6",
    "@vitest/coverage-v8": "^1.0.0",
    "jsdom": "^23.2.0",
    "typescript": "^5.2.0",
    "vite": "^7.1.1",
    "vitest": "^3.2.4"
  },
  "peerDependencies": {
    "typescript": ">=5.0.0"
  },
  "peerDependenciesMeta": {
    "typescript": {
      "optional": true
    }
  },
  "keywords": [
    "ui",
    "responsive",
    "breakpoints",
    "media-queries",
    "container-queries",
    "responsive-design",
    "tachui",
    "typescript"
  ],
  "author": "tachUI Team",
  "license": "MPL-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/tach-UI/tachUI",
    "directory": "packages/responsive"
  }
}
