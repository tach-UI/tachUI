{
  "name": "@tachui/query",
  "version": "0.8.32",
  "description": "Backend-neutral reactive query layer for tachUI: request lifecycle, cache, mutations, and async streams",
  "homepage": "https://tachui.dev/",
  "type": "module",
  "sideEffects": false,
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rm -rf dist tsconfig.build.tsbuildinfo && vite build && tsc --project tsconfig.build.json",
    "build:dev": "rm -rf dist tsconfig.build.tsbuildinfo && vite build --mode development && tsc --project tsconfig.build.json",
    "build:prod": "rm -rf dist tsconfig.build.tsbuildinfo && vite build --mode production && tsc --project tsconfig.build.json",
    "dev": "vite build --watch",
    "test": "vitest run",
    "type-check": "tsc --project tsconfig.type-check.json",
    "clean": "rm -rf dist",
    "lint": "oxlint src",
    "size": "node ../../tools/check-size-budget.mjs @tachui/query",
    "valid": "clear && vitest run && bun run type-check && bun run lint && bun run build"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "tachui": {
    "sizeBudget": {
      "entry": "dist/index.js",
      "gzipBytes": 12288
    }
  },
  "dependencies": {
    "@tachui/core": "0.8.32"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.8.0",
    "vite": "^7.1.1",
    "vitest": "^3.2.4"
  },
  "keywords": [
    "tachui",
    "query",
    "cache",
    "async",
    "reactive"
  ],
  "author": "tachUI Team",
  "license": "MPL-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/tach-UI/tachUI",
    "directory": "packages/query"
  },
  "publishConfig": {
    "access": "public"
  }
}
