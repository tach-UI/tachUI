---
'@tachui/types': patch
---

Release canary for the trusted-publishing (OIDC) migration (#245): publishes the added `repository` package metadata through the GitHub Actions OIDC flow to verify the npmjs.com Trusted Publisher configuration before broader releases rely on it.
